[build]
builder = "nixpacks"
# Server only — static shop is on AWS S3
buildCommand = "npm ci"

[deploy]
startCommand = "npm run start:server"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
